# SteamRoulette (Next.js)

- Copy `.env.example` to `.env.local` and fill values
- Install deps: `npm install`
- Generate Prisma client: `npm run prisma:generate`
- Dev server: `npm run dev`
- Tests: `npm test`